---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="Banking & Finance Solutions">
  <section class="min-h-screen flex items-center justify-center bg-slate-50 py-20">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto">
        <!-- Main Card Container -->
        <div class="bg-white rounded-2xl shadow-xl overflow-hidden">
          <!-- Hero Image -->
          <div class="relative h-[400px]">
            <img 
              src="https://source.unsplash.com/1600x900/?banking,finance" 
              alt="Banking & Finance Solutions" 
              class="w-full h-full object-cover"
            />
            <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent"></div>
          </div>

          <!-- Content Section -->
          <div class="p-8">
            <h1 class="text-4xl font-bold text-gray-800 mb-6 text-center">
              Improve Business Efficiency while Enhancing Security
            </h1>
            
            <div class="space-y-6 text-gray-600 mb-8">
              <p class="leading-relaxed">
                With millions of transactions every day, banking and financial institutions are continuously exposed to criminal threats. At the same time, improving the customer experience and management efficiency has emerged as a major demand.
              </p>
              <p class="leading-relaxed">
                The Dahua Banking & Finance Solution utilizes the latest technologies to secure banking transactions and improve management efficiency, keeping customers satisfied and your business running smoothly.
              </p>
            </div>

            <!-- Features List -->
            <ul class="space-y-3 mb-8 text-gray-700">
              <li class="flex items-center space-x-3">
                <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <span>Crystal Clear Images: 4K resolution presents a highly detailed image</span>
              </li>
              <li class="flex items-center space-x-3">
                <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <span>Smart Transaction Record: Records transaction information to provide strong evidence</span>
              </li>
              <li class="flex items-center space-x-3">
                <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <span>Queue Management: Measures and analyzes queues for better management decisions</span>
              </li>
            </ul>

            <!-- CTA Button -->
            <div class="text-center">
              <a 
                href="/Banking-Finance/banking-details" 
                class="inline-block px-8 py-3 bg-blue-600 text-white rounded-full hover:bg-blue-700 transition-all duration-300 transform hover:scale-105"
              >
                Learn More
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  section {
    animation: fadeIn 1s ease-out;
  }
</style>